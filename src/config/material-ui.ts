import { ThemeOptions } from '@material-ui/core/';

const materialUIOptions: ThemeOptions = {
  palette: {
    primary: {
      main: '#4caf50'
    },
    secondary: {
      main: '#2e7d32'
    }
  }
};

export default materialUIOptions;
